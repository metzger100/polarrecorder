{
  "note": "Machine-readable final public command graph. This is the target design; package.json activates each group only once its underlying tools/checks are genuinely clean, in the milestone order recorded below; the final promotion deleted check:migration once every group was activated.",
  "activationPhaseByGroup": {
    "setup": "1",
    "actions:lint": "1",
    "typecheck:migration-tests": "0/1 (temporary; later retired, replaced by typecheck:tests)",
    "format": "2",
    "format:check": "2",
    "lint": "2",
    "duplication:js": "2/3",
    "duplication:python": "3",
    "duplication:check": "2/3",
    "check:standard": "2",
    "typecheck:source": "4",
    "typecheck:tests": "5",
    "typecheck": "8 (literal graph is typecheck:source + typecheck:tests + typecheck:python; activated once the complete set existed)",
    "test:node": "3/5",
    "test:python": "5 (formalized as a named npm script wrapping pytest)",
    "test:split": "5",
    "test:focus:check": "5",
    "check:python-contracts": "3/8 (initially activated check-py-contracts.py only; later folded in check-python-compat.py, check-py-dependencies.py, and check-runtime-contracts.py to match the literal graph's 'Python 3.9 + architecture + dependency + runtime contracts' composition)",
    "package:check": "8",
    "check:fast": "8",
    "check:complexity": "7",
    "check:scaling": "7",
    "check:core": "8 (literal graph, activated)",
    "test:coverage:check": "6",
    "check:all": "8 (exact check:core && test:coverage:check, activated; tools/check-all.sh is now a pure compatibility wrapper around it)",
    "check:strict": "8 (exact alias of check:all, activated)"
  },
  "finalCheckCoreComposition": "check:standard && typecheck && package:check && test:focus:check && check:smells && check:python-contracts && test:split && check:complexity && check:scaling && docs:check && check:filesize",
  "finalCheckAllComposition": "check:core && test:coverage:check",
  "notYetActivatedLeaves": []
}
