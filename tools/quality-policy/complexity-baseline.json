{
  "note": "Complexity budget baseline. This active ledger may contain only identities whose live finding remains above the strict limits (complexity 10, max-statements 40, max-depth 4, max-params 6) and matches or is below the recorded entry value; new violations cannot be baselined (a finding with no matching entry fails outright), existing entries may only stay equal or shrink, and resolved entries must be removed in the same change. There is no historical Git-blob-derived exception path. All findings ever captured during the migration (viewer/export-ui.js, viewer/polar-chart.js x2, viewer/viewer.js) were already fixed, so this ledger starts empty. See tools/quality-policy/complexity-budget.mjs and documentation/conventions/quality-gates.md.",
  "entries": []
}
