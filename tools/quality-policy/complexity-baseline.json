{
  "note": "Complexity budget baseline. This active ledger may contain only identities from the immutable baseline capture (complexity-findings-capture.json) that remain above the strict limits (complexity 10, max-statements 40, max-depth 4, max-params 6). New violations cannot be baselined; existing entries may only stay equal or shrink; resolved entries must be removed in the same change. All findings present in the frozen capture (viewer/export-ui.js, viewer/polar-chart.js x2, viewer/viewer.js) were already fixed, so this ledger starts empty. See tools/quality-policy/complexity-budget.mjs and documentation/conventions/quality-gates.md.",
  "entries": []
}
